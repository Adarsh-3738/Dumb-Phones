<%- include("../partials/admin/header") %>
<link rel="stylesheet" href="/css/admin/edit-product.css">
<!-- Cropper.js -->
<link
  rel="stylesheet"
  href="https://unpkg.com/cropperjs@1.6.1/dist/cropper.min.css"
/>


<div class="card">
  <h2>Edit Product</h2>

  <form
    id="editProductForm"
    action="/admin/products/edit/<%= product._id %>"
    method="POST"
    enctype="multipart/form-data"
  >
    <label>Product Name</label>
    <input type="text" name="productName" value="<%= product.productName %>" required />

   <label>Regular Price</label>
<input type="number" name="regularPrice" value="<%= product.regularPrice %>" min="1" required />

<label>Sales Price</label>
<input type="number" name="salesPrice" value="<%= product.salesPrice %>" min="1" required />

<label>Stock</label>
<input type="number" name="quantity" value="<%= product.quantity %>" min="1" required />

    <label>Color</label>
    <input type="text" name="color" value="<%= product.color %>" />

    
    <label>Description</label>
    <textarea name="description"><%= product.description %></textarea>

    <label>Brand</label>
    <select name="brand">
      <% brands.forEach(b => { %>
        <option value="<%= b._id %>" <%= b._id.equals(product.brand._id) ? "selected" : "" %>>
          <%= b.name %>
        </option>
      <% }) %>
    </select>

    <label>Category</label>
    <select name="category">
      <% categories.forEach(c => { %>
        <option value="<%= c._id %>" <%= c._id.equals(product.category._id) ? "selected" : "" %>>
          <%= c.name %>
        </option>
      <% }) %>
    </select>

    <!-- EXISTING IMAGES -->
    <label>Existing Images</label>
    <div class="image-grid">
     <% if (product.productImage && product.productImage.length) { %>
  <% product.productImage.forEach(img => { %>

        <div class="image-box">
          <img src="<%= img.url %>">
          <button
            type="button"
            class="remove-img-btn"
            onclick="removeImage('<%= product._id %>', '<%= img.public_id %>', this)"
          >×</button>
        </div>
       <% }) %>
<% } else { %>
  <p style="color:#6b7280;">No images uploaded</p>
<% } %>

    </div>

    <label>Add New Images</label>
    <input type="file" id="imageInput" name="images" multiple accept="image/*" />

    <button type="submit" class="submit-btn">Update Product</button>
    <button type="button" class="cancel-btn" onclick="window.history.back()">Cancel</button>
  </form>
</div>

<div class="toast" id="toast"></div>

<!-- Cropper.js JS -->
<script src="https://unpkg.com/cropperjs@1.6.1/dist/cropper.min.js"></script>

<script src="/js/admin/edit-product.js"></script>

<%- include("../partials/admin/footer") %>
